# Porta em que a API vai escutar
PORT=3000

# Segredo para assinatura dos JWTs (use algo longo em produção)
JWT_SECRET=segredo-do-lab-nao-use-em-producao

# Tempo de expiração do JWT (ex.: 15m, 1h, 1d)
# Use valores curtos (ex.: 10s) para exercitar cenários de token expirado
JWT_EXPIRY=15m
