# Copie este arquivo para .env e ajuste os valores
# cp .env.example .env

# ── API ─────────────────────────────────────────────────────────────────────
PORT=3000
JWT_SECRET=segredo-do-lab-nao-use-em-producao
# Use valores curtos para simular token expirado, ex.: 10s
JWT_EXPIRY=15m
